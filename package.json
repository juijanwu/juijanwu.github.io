{
  "name": "web-clock",
  "version": "1.0.0",
  "description": "Web-based clock application",
  "type": "module",
  "scripts": {
    "test": "jest --run"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.15.0"
  }
}
